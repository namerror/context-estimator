# Summary

Updated `README.md` with a concise repo-structure section focused on the extension runtime, file groupings, and how `content.js` composes the rest of the JavaScript modules.

# Changes

- Added a grouped repo overview under `## Repo Structure`.
- Documented the main runtime flow from `manifest.json` through the shared globals and content modules into `content.js`.
- Added a Mermaid diagram showing the runtime relationship between `content.js` and the other loaded scripts.

# Tests

- Not run (not requested)

# Next

- Preview the README where Mermaid is rendered to confirm the diagram remains readable in the intended GitHub/docs surface.
