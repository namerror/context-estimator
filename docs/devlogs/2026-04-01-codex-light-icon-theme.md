## Summary
Shifted the extension from the dark GPT-inspired palette to a light theme that matches the icon’s soft white, teal, and green tone. The overlay and learn page keep the same layout and interaction model.

## Changes
- Reworked `styles.css` toward a bright neutral canvas with teal/green accents and charcoal text.
- Updated `learn.css` to use the same lighter surfaces, borders, and hover states for consistency.
- Kept the UI structure unchanged and preserved the existing responsive breakpoints.

## Tests
Not run (not requested).

## Next
- Open the overlay and `learn.html` to confirm the light palette aligns with `icon.png`.
- Tweak any contrast edge cases if the panel feels too soft against ChatGPT’s page chrome.
